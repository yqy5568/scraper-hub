services:
  # ——— 示例：yunqy 的 github-trending ———
  yunqy-github-trending:
    build:
      context: .
      args:
        OWNER: yunqy
        PROJECT: github-trending
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: "no"

  # ——— 添加更多爬虫服务 ———
  # zhangsan-douyin-video:
  #   build:
  #     context: .
  #     args:
  #       OWNER: zhangsan
  #       PROJECT: douyin-video
  #   env_file: .env
  #   volumes:
  #     - ./data:/app/data
  #   restart: unless-stopped
